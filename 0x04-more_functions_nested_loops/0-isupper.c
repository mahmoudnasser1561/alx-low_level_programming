#include <stdio.h>

/**
 * _isupper - check if a letter is upper or not
 * @c: the letter to be checked
 * Return: 1 for upper letter and 0 for any else
 */

int _isupper(int c)
{
	if (x >= 65 && x <= 90)
	{
		return (1);
	}
	else (0);
}
